# ============================================================
# ARHAM PORTO — ENVIRONMENT VARIABLES TEMPLATE
# ============================================================
# Do NOT commit sensitive values to version control.
# Copy this file to .env or .env.local for local development.

# Application Environment (development, test, production)
APP_ENV=development

# Go API Configuration
API_PORT=8080
WEB_ORIGIN=http://localhost:3000

# PostgreSQL Database (Phase 4+ runtime dependency)
DATABASE_URL=postgres://arham_user:arham_password@localhost:5432/arham_porto?sslmode=disable
POSTGRES_USER=arham_user
POSTGRES_PASSWORD=arham_password
POSTGRES_DB=arham_porto
POSTGRES_PORT=5432

# AI / LLM Configuration (Phase 6 Ask Arham AI Grounded Reviewer Copilot)
AI_MODE=disabled
AI_PROVIDER=
AI_MODEL=
AI_THINKING_LEVEL=low
AI_REQUEST_TIMEOUT_SECONDS=30
AI_MAX_CONCURRENT_REQUESTS=4
AI_RATE_LIMIT_PER_MINUTE=5
AI_MAX_EVIDENCE_CHARS=24000
GEMINI_API_KEY=

# Embedding Provider (Phase 5 Evidence Indexing & Retrieval)
# Non-secret placeholders only (Gate #25, #59). Never commit real credentials.
EMBEDDING_MODE=disabled
EMBEDDING_PROVIDER=
EMBEDDING_MODEL=
EMBEDDING_DIMENSIONS=768
GEMINI_API_KEY=

# GitHub API Configuration (Phase 5+)
GITHUB_TOKEN=

# Frontend Public Variables
NEXT_PUBLIC_API_BASE_URL=http://localhost:8080
NEXT_PUBLIC_MAP_STYLE_URL=https://demotiles.maplibre.org/style.json
