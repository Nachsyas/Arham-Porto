# Styles Directory

This directory contains application-level styles.
All canonical design tokens must originate from `packages/design-tokens` to maintain a single source of truth.
Global CSS is configured in `app/globals.css`.
