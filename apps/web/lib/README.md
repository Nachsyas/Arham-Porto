# Lib Directory

This directory contains client utilities, formatting helpers, and API client stubs.
Shared schemas and data models must be imported from `packages/content-schema` and `packages/portfolio-data`.
